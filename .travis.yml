language: android
jdk: oraclejdk8
sudo: false

android:
  components:
    - platform-tools
    - tools
    - build-tools-26.0.2
    - android-27
    - extra-android-m2repository

jobs:
  include:
    - stage: test
    script : ./gradlew :app:testDebugUnitTest
    - stage: build
    script: ./gradlew :app:assembleDebug